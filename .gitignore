# 의존성 / 빌드 산출물
node_modules
dist

# 환경변수 — 실제 키가 담기므로 절대 커밋 금지 (.env.example 만 커밋)
.env
.env.*
!.env.example

# 로컬 전용 파일
*.local
.claude/settings.local.json

# Vercel 배포 메타데이터
.vercel

# 로그 / OS 파일
npm-debug.log*
*.log
.DS_Store
